#include <stdio.h>

int main(void) {
    // Inicializa uma variável i iniciando com 0
    // Até que ela seja menor ou igual a 10
    // Incrementa 1 a cada ciclo
    for(int i=0; i<=10; i++){
        // Repete a apresentação do valor de I no console
        printf("I = %i\n", i);
    }
    return 0;
}